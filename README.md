Min-Max Scaling : This is useful when you know the boundaries of your data and need values in a strict 0-to-1 range — common in neural networks.

Z-Score Scaling: Standardization handles outliers better than min-max scaling because it is based on spread, not boundaries. 
It is the default choice for most classical machine learning algorithms.

